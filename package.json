{
  "name": "ipam-app",
  "version": "1.0.0",
  "description": "IPAM app - React + FastAPI Application",
  "scripts": {
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "uvicorn databricks_app:app --host 0.0.0.0 --port 8000"
  },
  "keywords": ["databricks", "react", "fastapi"],
  "license": "ISC"
}
